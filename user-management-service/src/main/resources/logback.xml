<configuration>
    <property name="CONSOLE_LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %loggerColor{2} - %msg%n"/>

    <conversionRule conversionWord="highlight" converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="loggerColor" converterClass="org.tasker.common.logging.CustomLoggerColorConverter"/>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

<!--    <appender name="FILE" class="ch.qos.logback.core.FileAppender">-->
<!--        <file>logs/my-project.log</file>-->
<!--        <encoder>-->
<!--            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
<!--        </encoder>-->
<!--    </appender>-->

    <logger name="org.tasker.usermanagementservice" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
<!--        <appender-ref ref="FILE"/>-->
    </logger>

    <!-- Корневой логгер (для всего остального) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>